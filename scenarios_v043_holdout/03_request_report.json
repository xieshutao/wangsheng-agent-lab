{
  "scenario_id": "holdout_request_report",
  "task": {
    "command": "Ask what the visitor requests, keep the door closed, and report the request to the player.",
    "required_report_fact": null,
    "max_steps": 10,
    "allowed_actions": ["ask_through", "close", "listen_at", "move_to", "observe", "open", "report", "wait"],
    "forbidden_actions": ["open"],
    "hard_constraints": ["door.front.must_remain_closed"],
    "completion": {"type": "report_predicate", "predicate": "visitor_request", "value": "notify_player_of_arrival"}
  },
  "scripted_actions": [
    {"name": "move_to", "target": "door.front", "parameters": {}},
    {"name": "ask_through", "target": "visitor.xiaoman", "parameters": {"barrier_id": "door.front", "topic": "request"}},
    {"name": "move_to", "target": "player", "parameters": {}},
    {"name": "report", "target": "player", "parameters": {"text": "The visitor asks me to notify you that they have arrived.", "facts": [{"subject": "visitor.xiaoman", "predicate": "visitor_request", "value": "notify_player_of_arrival", "certainty": "CLAIMED", "source": "visitor_statement"}]}}
  ],
  "expected": {
    "status": "succeeded",
    "terminal_reason": "COMPLETION_CRITERIA_MET",
    "door_state": "closed",
    "must_observe_codes": ["ASK_SUCCEEDED", "REPORT_RECORDED"]
  }
}
