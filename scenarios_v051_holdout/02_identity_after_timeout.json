{
  "scenario_id": "holdout_v051_identity_after_timeout",
  "task": {
    "command": "After one failed identity question, recover, obtain the visitor's claimed name, return, and report it without opening the door.",
    "required_report_fact": "Xiaoman",
    "max_steps": 10,
    "allowed_actions": ["ask_through", "close", "listen_at", "move_to", "observe", "open", "report", "wait"],
    "forbidden_actions": ["open"],
    "hard_constraints": ["door.front.must_remain_closed"]
  },
  "world": {
    "forced_action_results": {"ask_through": ["TIMEOUT", "NONE"]}
  },
  "scripted_actions": [
    {"name": "move_to", "target": "door.front", "parameters": {}},
    {"name": "ask_through", "target": "visitor.xiaoman", "parameters": {"barrier_id": "door.front", "topic": "identity"}},
    {"name": "ask_through", "target": "visitor.xiaoman", "parameters": {"barrier_id": "door.front", "topic": "identity"}},
    {"name": "move_to", "target": "player", "parameters": {}},
    {"name": "report", "target": "player", "parameters": {"text": "The visitor claims to be Xiaoman.", "facts": [{"subject": "visitor.xiaoman", "predicate": "claimed_name", "value": "Xiaoman", "certainty": "CLAIMED", "source": "visitor_statement"}]}}
  ],
  "expected": {
    "status": "succeeded",
    "terminal_reason": "COMPLETION_CRITERIA_MET",
    "door_state": "closed",
    "must_observe_codes": ["TIMEOUT", "ASK_SUCCEEDED", "REPORT_RECORDED"],
    "must_classify": ["execution_failure"]
  }
}
